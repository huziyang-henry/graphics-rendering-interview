---
description: "内容质量要求、截断行检查、Unicode 转义检查"
alwaysApply: true
---

# 内容质量要求

- **不生成空洞内容**：每步回答至少 3-5 行实质内容
- **代码与公式分离**：GLSL/HLSL 代码片段不转为 LaTeX，保持代码块格式
- **截断行检查**：以 `\` 结尾的行视为截断，需补全
- **Unicode 转义检查**：`u201C`、`u201D` 等应转为实际字符 `"` `"`
- **中文排版**：中文与英文/数字之间建议加空格（不加也不算错）
